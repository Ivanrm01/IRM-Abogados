export const metadata = {
  title: 'Admin | IRM Tax & Legal',
}

// The admin panel has its own full-page layout, no nav/footer
export default function AdminLayout({ children }) {
  return <>{children}</>
}
